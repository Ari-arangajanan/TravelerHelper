package com.MITProject.TravelerHelperBot.botController;import com.MITProject.TravelerHelperBot.auth.JwtUtil;import com.MITProjectService.bot.Services.SnUserService;import com.MITProjectService.bot.domain.SnUser;import jakarta.annotation.Resource;import jakarta.servlet.http.HttpServletRequest;import org.springframework.http.HttpStatus;import org.springframework.http.ResponseEntity;import org.springframework.web.bind.annotation.*;import java.util.Optional;@RestController@RequestMapping("app/user")public class UserController {    @Resource    private SnUserService userService;    // get user by id    @GetMapping("/getMe")    public ResponseEntity<?> getUserById(HttpServletRequest httpServletRequest) {        Long userId = JwtUtil.getUserIdByToken(httpServletRequest.getHeader("Authorization").replace("Bearer ", ""));        Optional<SnUser> user =  userService.getUserByID(userId);        return ResponseEntity.ok(user.isPresent() ? user : new ResponseEntity<>(HttpStatus.NOT_FOUND));    }    // update User    @PostMapping("/updateAvatar")    public ResponseEntity<?> updateUser(@RequestBody SnUser user, HttpServletRequest httpServletRequest) {        Long userId = JwtUtil.getUserIdByToken(httpServletRequest.getHeader("Authorization").replace("Bearer ", ""));        Optional<SnUser> updatedUser = Optional.ofNullable(userService.updateAvatar(userId, user.getAvatar()));        return ResponseEntity.ok(updatedUser.isPresent() ? updatedUser : new ResponseEntity<>(HttpStatus.EXPECTATION_FAILED));    }}