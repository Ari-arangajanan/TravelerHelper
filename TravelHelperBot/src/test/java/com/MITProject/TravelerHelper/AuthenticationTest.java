package com.MITProject.TravelerHelper;import com.MITProject.TravelerHelperBot.auth.JwtUtil;import com.MITProjectService.bot.Services.SnUserService;import com.MITProjectService.bot.Services.impl.SnUserServiceImpl;import com.MITProjectService.bot.dao.jpaRepos.UserRepository;import com.MITProjectService.bot.domain.SnUser;import static org.junit.jupiter.api.Assertions.*;import static org.mockito.Mockito.*;import org.junit.jupiter.api.Test;import org.junit.jupiter.api.extension.ExtendWith;import org.mockito.Mock;import org.mockito.junit.jupiter.MockitoExtension;import java.util.Optional;@ExtendWith(MockitoExtension.class)public class AuthenticationTest {    @Mock    private UserRepository userRepository;    @Mock    private SnUserServiceImpl userService;    @Mock    private JwtUtil jwtUtil;    @Test    public void testGenerateToken() {        Long telegramId = 6059382143L;        // Arrange: Mock user retrieval        SnUser mockUser = new SnUser();        mockUser.setId(2L);        mockUser.setTelegramId(telegramId);        mockUser.setUserName("testUser");        mockUser.setEmail("test@example.com");        when(userService.getUserByTelegrmId(telegramId)).thenReturn(Optional.of(mockUser));        System.out.println("Test generate token");        Optional<SnUser> user = userService.getUserByTelegrmId(telegramId);        // Assert: Verify that the user exists        assertTrue(user.isPresent());        assertEquals(2L, user.get().getId());        assertEquals("testUser", user.get().getUserName());        assertEquals("test@example.com", user.get().getEmail());        System.out.println("Test User: " + user.get().getId() + " " + user.get().getUserName() + " " + user.get().getEmail());        // Verify that the service method was called once        verify(userService, times(1)).getUserByTelegrmId(telegramId);    }}