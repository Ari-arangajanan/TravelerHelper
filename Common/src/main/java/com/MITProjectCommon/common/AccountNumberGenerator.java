package com.MITProjectCommon.common;import java.util.Random;/** * <h1>Account Number Generator</h1> * <p> * Utility class for generating unique 10-digit account numbers. * Ensures that the generated account number always has 10 digits * and does not start with zero. * </p> */public class AccountNumberGenerator {    // Constant defining the length of the account number    private static final int ACCOUNT_NUMBER_LENGTH = 10;    /**     * Generates a unique 10-digit account number.     * <p>     * This method ensures:     * - The first digit is never zero (to maintain a valid 10-digit number).     * - The remaining 9 digits are randomly generated (0-9).     * - The output is always a string containing exactly 10 digits.     * </p>     *     * @return A unique 10-digit account number as a String.     */    public static String generateUniqueAccountNumber() {        Random random = new Random();        // Ensure first digit is non-zero to make it a 10-digit number        StringBuilder accountNumber = new StringBuilder();        accountNumber.append(random.nextInt(9) + 1); // First digit (1-9)        // Generate remaining 9 digits (0-9)        for (int i = 1; i < ACCOUNT_NUMBER_LENGTH; i++) {            accountNumber.append(random.nextInt(10));        }        return accountNumber.toString();    }}