package com.MITProjectAdmin.Controller.management;import com.MITProjectService.bot.Services.BookingService;import com.MITProjectService.bot.dao.jpaRepos.specification.BookingFilterSpecification;import com.MITProjectService.bot.domain.Bookings;import com.MITProjectService.bot.request.BookingRequest;import jakarta.annotation.Resource;import org.springframework.data.domain.PageRequest;import org.springframework.data.domain.Pageable;import org.springframework.data.jpa.domain.Specification;import org.springframework.http.ResponseEntity;import org.springframework.web.bind.annotation.*;@CrossOrigin(origins = "http://localhost:5174", allowCredentials = "true")@RestController@RequestMapping(path = "/admin/bookings")public class BookingsManagementController {    @Resource    private BookingService bookingService;    // Get all Bookings    @PostMapping("/index")    public ResponseEntity<?> getAllBookings(@RequestBody BookingRequest request) {        Pageable pageable = PageRequest.of(request.getPage(), request.getLimit());        Specification<Bookings> filter = BookingFilterSpecification.bookingFilter(request);        return ResponseEntity.ok(bookingService.getAllBookingsPage(pageable, filter));    }}