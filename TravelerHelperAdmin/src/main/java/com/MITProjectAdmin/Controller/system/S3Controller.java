package com.MITProjectAdmin.Controller.system;import com.MITProjectService.framework.bucket.S3Service;import jakarta.annotation.Resource;import org.springframework.http.MediaType;import org.springframework.http.ResponseEntity;import org.springframework.web.bind.annotation.*;import javax.imageio.ImageIO;import java.awt.image.BufferedImage;import java.io.ByteArrayInputStream;import java.io.IOException;import java.io.InputStream;@RestController@RequestMapping("/admin/S3files")public class S3Controller {    @Resource    private S3Service s3Service;    @PostMapping(value = "/image/upload", consumes = {MediaType.IMAGE_JPEG_VALUE, MediaType.IMAGE_PNG_VALUE, "image/gif"})    public ResponseEntity<String> uploadImageStream(@RequestHeader("Content-Type") String contentType,                                                    @RequestBody byte[] imageData) throws IOException {        // Validate image        InputStream inputStream = new ByteArrayInputStream(imageData);        BufferedImage image = ImageIO.read(inputStream);        if (image == null) {            return ResponseEntity.badRequest().body("Invalid image file.");        }        // Upload and return pre-signed URL        String imageUrl = s3Service.uploadFile(imageData, contentType);        return ResponseEntity.ok(imageUrl);    }    @GetMapping("/image/{fileName}")    public ResponseEntity<byte[]> downloadImage(@PathVariable String fileName) throws IOException {        byte[] imageData = s3Service.downloadFile(fileName);        return ResponseEntity.ok(imageData);    }    @DeleteMapping("/image/{fileName}")    public ResponseEntity<String> deleteImage(@PathVariable String fileName) {        s3Service.deleteFile(fileName);        return ResponseEntity.ok("File deleted successfully: " + fileName);    }}