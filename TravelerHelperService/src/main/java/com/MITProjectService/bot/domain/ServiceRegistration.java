package com.MITProjectService.bot.domain;import jakarta.persistence.*;import lombok.Data;import java.time.LocalDateTime;import java.util.List;@Data@Entity@Table(name = "service_registration")public class ServiceRegistration {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    @Column(name = "id")    private Long id;    @OneToOne    @JoinColumn(name = "service_id", nullable = false)    private Service service; // Links to Service    @ManyToOne    @JoinColumn(name = "registered_by", nullable = false)    private ServiceProvider registeredBy; // User who registered the service    @Column(name = "status", nullable = false)    private String status; // e.g., Pending, Approved, Rejected    @Column(name = "registration_date", nullable = false)    private LocalDateTime registrationDate;    @Column(name = "approval_date")    private LocalDateTime approvalDate;    @OneToMany(mappedBy = "serviceRegistration", cascade = CascadeType.ALL)    private List<ServiceAttribute> attributes; // List of dynamic attributes}