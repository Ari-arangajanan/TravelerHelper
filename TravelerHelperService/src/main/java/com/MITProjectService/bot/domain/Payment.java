package com.MITProjectService.bot.domain;import jakarta.persistence.*;import lombok.Data;import java.time.LocalDateTime;@Entity@Table(name = "payments")@Datapublic class Payment {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    private Long id;    @OneToOne(cascade = CascadeType.ALL)    @JoinColumn(name = "booking_id", nullable = false, unique = true)    private Bookings booking;    @Column(name = "amount", nullable = false)    private Double amount;    @Column(name = "payment_method", nullable = false)    private String paymentMethod; // e.g., CASH, CARD, ONLINE, PENDING at initial moment    @Column(name = "payment_gateway", nullable = false)    private String paymentGateway; // e.g., PAYPAL, STRIPE, RAZORPAY, PAYHERE,    @Column(name = "payment_orderId", nullable = false)    private String paymentOrderId; // e.g., Transaction ID    @Column(name = "payment_status")    private  Integer paymentStatus; // PAID 2, PENDING 1, FAILED 3, REFUNDED 4, Cancelled 5    @Column(name = "created_at", nullable = false)    private LocalDateTime createdAt;    @PrePersist    protected void onCreate() {        createdAt = LocalDateTime.now();    }}