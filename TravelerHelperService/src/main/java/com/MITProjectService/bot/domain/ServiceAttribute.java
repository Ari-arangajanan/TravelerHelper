package com.MITProjectService.bot.domain;import jakarta.persistence.*;import lombok.Data;@Data@Entity@Table(name = "service_attribute")public class ServiceAttribute {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    @Column(name = "id")    private Long id;    @ManyToOne    @JoinColumn(name = "registration_id", nullable = false)    private ServiceRegistration serviceRegistration; // Links to ServiceRegistration    @Column(name = "attribute_name", nullable = false)    private String attributeName; // e.g., room_type, capacity    @Column(name = "attribute_value", nullable = false)    private String attributeValue; // e.g., Suite, 4}