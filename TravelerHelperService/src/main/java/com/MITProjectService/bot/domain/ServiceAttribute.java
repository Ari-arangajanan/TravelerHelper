package com.MITProjectService.bot.domain;import com.fasterxml.jackson.annotation.JsonBackReference;import com.fasterxml.jackson.annotation.JsonIgnore;import com.fasterxml.jackson.annotation.JsonManagedReference;import jakarta.persistence.*;import lombok.Data;import lombok.ToString;@Data@Entity@Table(name = "service_attribute")public class ServiceAttribute {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    @Column(name = "id")    private Long id;    @ManyToOne    @JoinColumn(name = "serviceType_id",referencedColumnName = "id", nullable = false)    @JsonBackReference    @ToString.Exclude    @JsonIgnore    private ServiceType serviceType; // Links to ServiceRegistrationEnum    @Column(name = "attribute_name", nullable = false)    private String attributeName; // e.g., room_type, capacity    @Column(name = "attribute_value", nullable = false)    private String attributeValue;}