package com.MITProjectService.bot.request;import lombok.Data;import javax.validation.constraints.NotNull;@Datapublic class BookingRequest {    @NotNull(message = "Service Type ID is required.")    private Long serviceTypeId; // ID of the ServiceType being booked    @NotNull(message = "User Telegram ID is required.")    private Long userTelegramIdId; // ID of the User making the booking    @NotNull(message = "Service ID is required.")    private Long serviceId; // ID of the Service being booked    private int status; // Optional: PENDING, ACCEPTED, REJECTED}