package com.MITProjectService.bot.request;import com.MITProjectCommon.constants.Constants;import com.MITProjectService.bot.enums.BookingStatusEnum;import jakarta.persistence.Column;import lombok.Data;import javax.validation.constraints.NotNull;import java.time.LocalDateTime;@Datapublic class BookingRequest {    private int page=(int) Constants.DEFAULT_PAGE;    private int limit= (int) Constants.DEFAULT_LIMIT;    private Long bookingId;    @NotNull(message = "Service Type ID is required.")    private Long serviceTypeId; // ID of the ServiceType being booked    @NotNull(message = "User Telegram ID is required.")    private Long userTelegramIdId; // ID of the User making the booking    private Long userId; // ID of the User making the booking    private Long serviceProvider; // ID of the User making the booking    @NotNull(message = "Service ID is required.")    private Long serviceId; // ID of the Service being booked    @NotNull(message = "Service ID is required.")    private LocalDateTime bookingDateFrom;    @NotNull(message = "Service ID is required.")    private LocalDateTime bookingDateTo;    private BookingStatusEnum status; // Optional: PENDING, ACCEPTED, REJECTED    private String rejectReason;}