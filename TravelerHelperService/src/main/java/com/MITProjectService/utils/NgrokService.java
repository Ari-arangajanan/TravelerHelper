package com.MITProjectService.utils;import lombok.Getter;import org.springframework.beans.factory.annotation.Value;import org.springframework.stereotype.Service;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;@Getter@Servicepublic class NgrokService {    private String ngrokUrl;    // Inject the Ngrok token from application.yml    @Value("${ngrok.token}")    private String ngrokToken;    public NgrokService() {        startNgrok();    }    private void startNgrok() {        try {            // Start ngrok with authtoken and http tunnel on port 8080            Process process = new ProcessBuilder("ngrok", "http", "8080", "--authtoken", ngrokToken).start();            // Capture Ngrok console output            BufferedReader reader = new BufferedReader(new InputStreamReader(process.getInputStream()));            String line;            while ((line = reader.readLine()) != null) {                System.out.println("Ngrok output: " + line);  // Print each line of Ngrok output to console                if (line.contains("https://")) {                    ngrokUrl = line.substring(line.indexOf("https://")).split(" ")[0];                    System.out.println("Ngrok URL: " + ngrokUrl);                    break;                }            }        } catch (IOException e) {            e.printStackTrace();        }    }}